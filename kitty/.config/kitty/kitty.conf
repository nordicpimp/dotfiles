# kitty config — colors added by matugen in guide 4
font_family      JetBrainsMono Nerd Font
font_size        12.0
cursor_shape     beam
scrollback_lines 5000

# Allow remote control for live color reloading
allow_remote_control yes

include /home/nordicpimp/.cache/matugen/colors-kitty.conf
